trigger:
- develop

pool:
  vmImage: windows-latest

parameters:
- name: devIndex
  displayName: 'Index de l environnement cible (seulement pour un déploiement sur env de DEV)'
  type: number
  default: 0
- name: disableSonar
  type: boolean
  default: false
  values:
    - true
    - false
- name: deployType
  displayName: "Technical parameter : don t modify it ! onlyDev/standard"
  type: string
  default: 'standard'

resources:
  repositories:
  - repository: Pulse
    name: 'Pulse.Operations.Pipelines'
    type: git
    ref: 'refs/heads/Release/v2.0'

extends:
  template: pipelines/templates/platform.back.sql.yaml@Pulse
  parameters:
    devIndex: ${{ parameters.devIndex }}
    deployType: ${{ parameters.deployType }}
    disableSonar: ${{ parameters.disableSonar }}
    domain: reg
    resource:
      id: '01'
      name: 'reg'
      path: src\ContactRegistry.Sql.Database\ContactRegistry.Sql.Database.sqlproj
    dotnetcore: 8.x